import { ApiController } from "../controllers/apiController";
import { Config } from "../utils/config";

export interface RootRouter {
  // apiRouter: Api
}
export const getRootRouter = (config: Config) => {};
